/**
 * The default results per page for the search results.
 */
export const defaultMaxResultsPerPage = 5;

/**
 * The minimum characters required to trigger a search.
 */
export const minimumCharactersForSearch = 3;